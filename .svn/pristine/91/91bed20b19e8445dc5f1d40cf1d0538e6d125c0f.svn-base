using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Pharos.Logic.InstalmentDomain.Interfaces
{
    public interface IInstalmentRuleProvider
    {
        /// <summary>
        /// 用于权限过滤，或者其他过滤
        /// </summary>
        /// <param name="parameter">参数</param>
        /// <returns>是否允许分期</returns>
        bool Filter(object parameter);
        /// <summary>
        /// 加载规则
        /// </summary>
        /// <param name="parameter">参数</param>
        /// <returns>分期规则</returns>
        IInstalmentRule LoadRule(object parameter);

        IEnumerable<IInstalmentItem> Run(object parameter);

        IEnumerable<IInstalmentItem> Run(IEnumerable<object> parameter);
    }
}
