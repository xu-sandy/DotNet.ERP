using Pharos.POS.Retailing.Models;
using System;
using System.Windows;
using System.Windows.Data;

namespace Pharos.POS.Retailing
{
    public class IsEditConverter : IValueConverter
    {
        public object Convert(object value, Type targetType, object parameter, System.Globalization.CultureInfo culture)
        {
            var product = (Product)value;
            if (parameter == null)
            {
                return (product.IsEdit) ? Visibility.Visible : Visibility.Hidden;
            }
            else
            {

                switch (parameter.ToString())
                {
                    case "Sale":
                        if (product.EnableEditSale)
                        {
                            return (product.IsEdit) ? Visibility.Visible : Visibility.Hidden;

                        }
                        else
                        {
                            return Visibility.Hidden;
                        }
                    case "Num":
                        if (product.EnableEditNum)
                        {
                            return (product.IsEdit) ? Visibility.Visible : Visibility.Hidden;

                        }
                        else
                        {
                            return Visibility.Hidden;
                        }
                }
                return Visibility.Hidden;

            }
        }

        public object ConvertBack(object value, Type targetType, object parameter, System.Globalization.CultureInfo culture)
        {
            return ((Visibility)value) == Visibility.Visible ? true : false;
        }
    }
}