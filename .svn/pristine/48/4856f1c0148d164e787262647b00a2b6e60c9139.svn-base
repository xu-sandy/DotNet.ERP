using Pharos.Logic.BLL;
using Pharos.Service.Retailing.Marketing;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Pharos.Service.Retailing
{
    class Program
    {
        static void Main(string[] args)
        {
            var stores = WarehouseService.GetStores();
            foreach (var item in stores)
            {
                MarketingManager marketingManager = new MarketingManager(item.StoreId, item.CompanyId);
            }
            Console.WriteLine("促销启动！");
            Console.ReadLine();
        }
    }
}
