using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Pharos.Logic.DataSync
{
    /// <summary>
    /// 将POS端的出入款信息上传更新到管理端
    /// </summary>
    public class IncomePayoutToManage
    {
        /// <summary>
        /// 在线直接更新出入款信息到管理端（由POS端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OnlineUpdateManage()
        {
            //todo: 上传后，POS本地只保留近6个月的数据
            return true;
        }

        /// <summary>
        /// 离线导入更新出入款信息到POS端（由管理端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OfflineImportUpdatePos()
        {
            //todo:
            return true;
        }

        /// <summary>
        /// 离线导出更新出入款信息到excel文件（由POS端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OfflineExportExcel()
        {
            //todo:
            return true;
        }
    }
}
