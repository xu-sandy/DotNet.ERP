using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Pharos.Logic.DataSync
{
    /// <summary>
    /// 将售后退换信息上传更新到管理端
    /// </summary>
    public class SalesReturnsToManage
    {
        /// <summary>
        /// 在线直接更新售后退换信息到管理端（由POS端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OnlineUpdateManage()
        {
            //todo: 
            // 1）上传新增的售后退换信息；
            // 2）上传成功后不保留本地数据。
            return true;
        }

        /// <summary>
        /// 离线导入更新售后退换信息到POS端（由管理端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OfflineImportUpdatePos()
        {
            //todo:
            return true;
        }

        /// <summary>
        /// 离线导出更新售后退换信息到excel文件（由POS端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OfflineExportExcel()
        {
            //todo:
            return true;
        }
    }
}
