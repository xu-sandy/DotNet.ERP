using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Pharos.Logic.DataSync
{
    /// <summary>
    /// 将会员基本信息在两端同步
    /// </summary>
    public class MemberToManage
    {
        /// <summary>
        /// 在线直接更新会员基本信息到管理端（由POS端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OnlineUpdateManage()
        {
            //todo: 
            // 1）上传新增的会员信息；
            // 2）从管理端下载所有会员到本地POS，确保其他门店的会员也能在本门店识别；
            // 3）上传会员积分明细，上传成功后不保留本地数据。
            return true;
        }

        /// <summary>
        /// 离线导入更新会员基本信息到POS端（由管理端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OfflineImportUpdatePos()
        {
            //todo:
            return true;
        }

        /// <summary>
        /// 离线导出更新会员基本信息到excel文件（由POS端发起调用）
        /// </summary>
        /// <returns>true:成功，false:失败</returns>
        public bool OfflineExportExcel()
        {
            //todo:
            return true;
        }
    }
}
