<UserControl x:Class="Pharos.Barcode.Retailing.ChildTabContentControls.SystemManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="150"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <GroupBox Header="服务器设置" Name="serverSettings">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="120"></ColumnDefinition>
                    
                </Grid.ColumnDefinitions>
                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center">服务器地址：</TextBlock>
                <TextBox Text="{Binding ServerUrl}" Grid.Column="1" Height="30" MinWidth="240"  VerticalContentAlignment="Center" HorizontalAlignment="Left"></TextBox>
                <Button Grid.Column="2" Height="30" Width="120" Command="{Binding SaveCommand}">设置</Button>
            </Grid>
        </GroupBox>
        <GroupBox Name="printerSettings" Grid.Row="1" Header="打印机设置">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="35"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="35"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock  HorizontalAlignment="Right" Grid.Row="1"  VerticalAlignment="Center">选择打印机：</TextBlock>
                <ComboBox Grid.Row="1" VerticalContentAlignment="Center" ItemsSource="{Binding RunningBarCodePrinters}" SelectedItem="{Binding CurrentBarCodePrinter}" Grid.Column="1" Width="300" HorizontalAlignment="Left"></ComboBox>
                <TextBlock  HorizontalAlignment="Right" Grid.Row="3" VerticalAlignment="Center">打印模板：</TextBlock>
                <ComboBox VerticalContentAlignment="Center" HorizontalContentAlignment="Left" DisplayMemberPath="Key" ItemsSource="{Binding PrinterSettingGroups}" SelectedItem="{Binding CurrentSettings}"  Grid.Column="1" Grid.Row="3" Width="300" HorizontalAlignment="Left"></ComboBox>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
